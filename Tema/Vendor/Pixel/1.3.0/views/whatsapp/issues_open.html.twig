Nova Issue aberta no repositório *{{ repositorio.name }}*.

A issue de número *#{{ issue.number }} - {{ issue.title }}* ({{ issue.html_url|raw }}) acaba de ser aberta por *@{{ user.login }}* em *{{ data|raw }}*, às *{{ hora|raw }}*.
{% if assignedes is not empty %}

    {{ assigneds|raw }}
{% endif %}
----