Nova release gerada no repositório [{{ repositorio.name }}]({{ repositorio.html_url|raw}}).

A tag [@{{ tag|raw }} - {{ title|default('Não Informado')|raw }}]({{ releaseUrl|raw }}) acaba de ser lançada por [@{{ user.login }}]({{ user.html_url|raw }}) em *{{ data|raw }}*, às *{{ hora|raw }}*, com a seguinte descrição:

{{ descricao|raw }}
----